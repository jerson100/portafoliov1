import MenuBar from "../../../MenuBar";
import MobileMenuList from "../MobileMenuList";

const NavBar = () => {
  return (
    <div>
      {/* <MenuBar /> */}
      <MobileMenuList />
    </div>
  );
};

export default NavBar;
