import styled from "styled-components";

export const MenuToogleButtonStyle = styled.button`
  border: none;
  outline: none;
  cursor: pointer;
  background: transparent;
  position: relative;
  z-index: 1;
`;
